const tisPath = 'C:/temp/Text-in-scope/'

module.exports.PATHS = Object.freeze({
  tisPath: tisPath,
  completePath: `${tisPath}complete/`,
  utilsPath: `${tisPath}utils/`,
  logPath: `${tisPath}logs/`
})
