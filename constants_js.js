const tis_path = 'C:/temp/Text-in-scope/'

module.exports.PATHS = Object.freeze({
    tis_path : tis_path,
    complete_path : `${tis_path}complete/`,
    utils_path : `${tis_path}utils/`,
    log_path : `${tis_path}logs/`
})


